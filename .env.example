DATABASE_URL="file:./dev.db"
JWT_SECRET=""
AWS_REGION="us-east-1"
# Credentials for the dedicated bubld-tasks-app IAM user (scoped only to
# bubld-tasks-attachments/*, plus ses:SendEmail once notifications ship).
# Not needed if the AWS SDK can otherwise resolve credentials (e.g. a
# local ~/.aws profile) -- required on the deploy box, which has neither.
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
S3_ATTACHMENTS_BUCKET=""
SES_FROM_EMAIL="tasks@bubld.com"
